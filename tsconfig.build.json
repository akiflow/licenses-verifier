{
  "//": "Build config: compiles src/ to dist/. The tests are deliberately out of scope, they must never reach the published package.",
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "rootDir": "./src/",
    "outDir": "./dist/",
    "noEmit": false,
    "resolveJsonModule": false,
    "types": ["node"]
  },
  "include": [
    "src/**/*.ts"
  ],
  "exclude": [
    "node_modules",
    "dist",
    "test"
  ]
}
